{
  "version": 2,
  "buildCommand": "sh -c 'cd client && npm install && npm run build'",
  "outputDirectory": "client/dist",
  "installCommand": "sh -c 'cd client && npm install'",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

